openapi: 3.1.0
info:
  title: Mithras API
  version: 0.0.0
  description: |
    Mithras authentication provider API

servers:
  - url: http://localhost:8080

paths:
  /v1/echo:
    post:
      $ref: "./paths/v1_echo.yaml#/paths/post"

security:
  - rootKey: []
