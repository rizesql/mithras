openapi: 3.1.0
info:
  title: Mithras API
  version: 0.0.0
  description: |
    Mithras authentication provider API

servers:
  - url: http://localhost:8080

paths:
  /v2/register:
    post:
      $ref: "./paths/v2_register.yaml#/paths/post"

security:
  - rootKey: []
